<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @picture.title %>
</p>

<p>
  <strong>Image:</strong>
  <%= link_to image_tag(@picture.image.url(:medium)), @picture.image.url %>
  <!-- <img src="<%#= @picture.image.url(:medium) %>" alt=""> -->
</p>

<%= link_to 'Edit', edit_picture_path(@picture) %> |
<%= link_to 'Home', root_path %>
