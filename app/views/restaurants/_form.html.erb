<%= form_with(model: restaurant, local: true) do |form| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>g
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :restaurant_name %>
  </div>

  <div class="field">
    <%= form.label :busyness %>
    <%= form.number_field :busyness, id: :restaurant_busyness %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%#= form.number_field :price, id: :restaurant_price %>
    <%= form.select :price, options_for_select(["💰","💰💰","💰💰💰"].each{|u| [u, u] }) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
