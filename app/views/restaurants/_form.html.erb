<div class="col-md-4 col-md-offset-4">

<%= form_with(model: restaurant, local: true) do |form| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>g
    </div>
  <% end %>

  <%= form.hidden_field :user_id, id: :user_id, value: @current_user.id %>

  <div class="form-group">
    <%= form.label :name, "Restaurant Name", class: "new-restaurant" %>
    <%= form.text_field :name, id: :restaurant_name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :comment, "Rating", class: "new-restaurant" %>
    <%= form.select :price, options_for_select(["🙌🏼","🤷🏼","💩"].each{|u| [u, u] }), {}, {class: "form-control"}  %>  </div>

  <div class="form-group">
    <%= form.label :price, "Price", class: "new-restaurant" %>
    <%= form.select :price, options_for_select(["💰","💰💰","💰💰💰"].each{|u| [u, u] }), {}, {class: "form-control"}  %>
  </div>

  <div class="form-group">
    <%= form.submit "Create Restaurant", class: "btn btn-success" %>
  </div>
<% end %>

</div>
